/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost"],
  },
  basePath: "/frontend-challenges/notifications-page",
  output: "standalone",
};

export default nextConfig;
